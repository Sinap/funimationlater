============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install funimationlater
    $ pip install funimationlater

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv funimationlater
    $ pip install funimationlater
